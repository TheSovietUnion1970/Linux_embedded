sudo iptables -t nat -A POSTROUTING -o wlp1s0 -j MASQUERADE
sudo iptables -A FORWARD -i usb0 -o wlp1s0 -j ACCEPT
sudo iptables -A FORWARD -i wlp1s0 -o usb0 -m state --state RELATED,ESTABLISHED -j ACCEPT

=> The first line is IMPORTANT as it will translate the src of IP

==== After ping 8.8.8.8 successfully, add these to access Internet by hostname
echo "nameserver 1.1.1.1" | sudo tee /etc/resolv.conf
echo "nameserver 1.0.0.1" | sudo tee -a /etc/resolv.conf

