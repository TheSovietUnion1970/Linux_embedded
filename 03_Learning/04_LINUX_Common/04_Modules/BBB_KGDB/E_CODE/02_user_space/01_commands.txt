=============== Common
cat /proc/interrupts | grep 65
sudo cat /sys/kernel/debug/clk/clk_summary | grep 044

=============== SPI
ls /sys/class/spi_master/ 			-> inspect component has been present or not
ls -l /sys/class/spi_master/spi0/ 		-> show info
cat /sys/class/spi_master/spi0/uevent 		-> show which driver is bound to the node

=============== I2C
ls /sys/class/i2c-adapter 			-> ls to check the bus
cat /sys/kernel/debug/pinctrl/pinctrl-maps 	-> cat to read
