        dma_addr = dma_map_single(data->dev, buf_test, count, DMA_TO_DEVICE);
        // dma_addr = dma_map_single(data->dev, msg1, count, DMA_TO_DEVICE);
        if (dma_mapping_error(data->dev, dma_addr)) {
            dev_err(data->dev, "Failed to map source buffer for DMA\n");
            return -ENOMEM;
        }