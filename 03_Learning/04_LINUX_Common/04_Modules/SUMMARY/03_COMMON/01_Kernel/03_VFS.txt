(5)
 - Cache: (dentry - path, page - file data)
 - journaling: flush/sync
 - permissions
 - uniform APIs
 - filesystems/executables[ELF or a.out] formats
 
VFS arch:
 - CallIf -> VFS -> filesystem (ext, fat)                       -> cache -> dev driv (char/blkc)
                                          -> binary (ELF, PE32) ->
 
i-node - data structure ~ metadata of a file in 
       - hide file system details
       - pointer to metadata -> proper operations

=== commands ====
cat /proc/meminfo:
 + Cached	 the bytes of dentry + file content
 + SwapCached	 the bytes of swapped between RAM and cache  
mount | grep vfat -> check filesystem format of a block
ls -i <file> -> check i-node number

=== CPU cache  >< Page cache ===
    HW	       >< OS
    <-> DRAM   >< <-> disk
    cache line >< page
      (64B)        (4kB)
    dat/ins    >< dat
    
 => free -h 
